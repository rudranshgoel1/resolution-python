[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week1_stolen_username
version = "1.0.0"
dependencies = []

[project.scripts]
todo = "resolution_week1_stolen_username.main:main"